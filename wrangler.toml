name = "grepbase"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "grepbase-db"
database_id = "YOUR_DATABASE_ID" # Replace after running: npx wrangler d1 create grepbase-db
migrations_dir = "drizzle"
